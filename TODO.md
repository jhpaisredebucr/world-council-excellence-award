# TODO

## Carousel slide images (Home)
- [ ] Add `imageSrc` placeholder field to `CAROUSEL_SLIDES` in `src/app/home/(main)/page.js`
- [ ] Render slide image via `next/image` using the provided `imageSrc` paths (overlayed with existing gradient)
- [ ] Verify homepage build/lint after changes

